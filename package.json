{
  "name": "harden",
  "version": "1.0.0",
  "description": "salary",
  "main": "index.ts",
  "scripts": {
    "clear": "rm -rf dist",
    "server": "NODE_ENV=production node ./server/bin/server.js",
    "start": "NODE_ENV=production node ./server/bin/server.js",
    "dev": "NODE_ENV=development nodemon --watch server ./server/bin/server.ts",
    "build": "npm run clear && NODE_ENV=production webpack --env production --config ./build/webpack.prod.config.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "salary"
  ],
  "author": "jinming1937@163.com",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^7.32.0",
    "nodemon": "^2.0.12",
    "prettier": "^2.3.2",
    "ts-node": "^10.2.0"
  },
  "dependencies": {}
}
